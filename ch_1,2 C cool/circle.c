#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("The area of this circle is %f \n ", pi * radius * radius);
    int height = 3;
    printf("The volume  of this clylinder is %f \n ", pi * radius * height);

    return 0;
}